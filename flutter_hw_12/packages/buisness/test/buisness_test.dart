import 'package:flutter_test/flutter_test.dart';

import 'package:buisness/buisness.dart';

void main() {
  test('adds one to input values', () {
    expect(find.text('Flutter_hw_12'), findsOneWidget);
  });
}
